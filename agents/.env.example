# Supabase — service role for agent pipeline (bypasses RLS)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Firecrawl — web crawling API
FIRECRAWL_API_KEY=

# Anthropic — Claude for content analysis
ANTHROPIC_API_KEY=
