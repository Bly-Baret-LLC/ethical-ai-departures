# CLAUDE.md — Warning Collective

## Data Accuracy

All dates, claims, and factual assertions displayed on the site MUST be verified against primary sources before being committed. Never use database `created_at` timestamps as proxies for when real-world events occurred. If a date cannot be verified, omit it rather than guess. This is an editorial platform — inaccuracy undermines the entire mission.
